import {PrismaDataController} from "./controller";

class DictionaryController extends PrismaDataController<number, any, any, any> {
    fetchSingleCustom() {
    }

    fetchSingleDetailed() {
    }

    update(input: any) {
    }

}
